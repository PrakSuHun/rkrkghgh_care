# 서류 탭 확장 계획

## 목표
서류 탭에서 회의록 외에 7종의 센터 운영 서류를 AI 자동 생성 + 인쇄 가능한 형태로 제공.

## 지원 서류 목록 (총 8종 + 회의록 8종)

### 대상자 기반
| # | 서류명 | AI 필요 | 입력 | 데이터 소스 |
|---|---|---|---|---|
| 1 | 욕구사정지 | ❌ | 어르신 선택 | seniors.assess_* 10영역 |
| 2 | 급여제공계획서 | ⭕ | 어르신 선택 | seniors + 담당 요양사 |
| 3 | 상태기록지(월간) | ⭕ | 어르신 + 년월 | 해당월 journals |
| 4 | 급여제공기록지 | ❌ | 어르신 + 년월 | 해당월 journals 나열 |
| 5 | 수급자 인수인계서 | ❌ | 어르신 선택 | 기본정보 + 욕구사정 요약 |

### 요양사 기반
| # | 서류명 | AI 필요 | 입력 | 데이터 소스 |
|---|---|---|---|---|
| 6 | 상담일지 월간보고 | ⭕ | 요양사 + 년월 | 해당월 counseling_logs |

### 매칭 기반
| # | 서류명 | AI 필요 | 입력 | 데이터 소스 |
|---|---|---|---|---|
| 7 | 서비스제공 계약서 | ❌ | 어르신 선택 (주담당 자동) | seniors + 주담당 요양사 |
| 8 | 월간 업무보고서 | ⭕ | 요양사 + 년월 | 담당 어르신별 journals 통합 |

(회의록 8종은 이미 구현됨)

## 아키텍처

### 저장 여부
- **회의록**: `meetings` 테이블 저장 (이미 구현)
- **나머지 7종**: **저장 안 함, 즉석 생성**
  - 이유: 원본 데이터가 바뀌면 재생성 필요하고, 출력만 하면 되는 서류라 DB 누적 가치 낮음
  - 동일 서류를 나중에 다시 뽑으면 그때 데이터 기준으로 재생성됨

### 파일 구조 (이미 일부 스캐폴드됨)
```
app/lib/docGenerator.ts        [스캐폴드됨] 8종 타입 디스패처
app/api/documents/generate/    [신규] POST {type, params} → 서류 JSON
app/documents/page.tsx         [수정] 서류 종류 카드 추가
app/documents/preview/page.tsx [신규] 쿼리스트링 받아 프리뷰+인쇄
app/documents/PickerDialog.tsx [신규] 어르신/요양사/년월 선택 모달
app/globals.css                [수정] @media print 스타일
```

### 출력 포맷
모든 서류는 공통 JSON 구조:
```ts
{
  title: string,        // "욕구사정지"
  subtitle?: string,    // "김영희"
  meta: [{label, text}], // 상단 메타정보 (성명·등급·작성일 등)
  sections: [{label, text}] // 본문 섹션들
}
```
프리뷰 페이지에서 이 구조 하나로 모두 렌더링.

### 인쇄/출력
- "인쇄" 버튼 → `window.print()`
- `@media print` CSS로 네비게이션/버튼 숨김, A4 여백 설정
- PDF가 필요하면 브라우저 "PDF로 저장" 사용 (별도 라이브러리 안 씀)

## 흐름
1. `/documents` 카드 클릭
2. 필요한 입력 받는 모달 (어르신/요양사/년월)
3. `/documents/preview?type=xxx&seniorId=...&month=...` 이동
4. 페이지 진입 시 API 호출 → 로딩 → 렌더
5. 상단 **인쇄** 버튼 + **전체 복사** 버튼

## 편집/수정 방식 (업데이트)

### 두 가지 방법 모두 지원
1. **직접 편집**: 각 섹션 텍스트를 바로 클릭해서 고침 (textarea)
2. **AI로 다듬기**: 하단 채팅창에 자연어로 지시
   - 예: "결정사항 부분 더 간결하게"
   - 예: "다음 회의 일정을 2026-05-15로 넣어줘"
   - 예: "종합의견에서 낙상 위험 문구를 부드럽게"
   - AI가 현재 서류 전체 + 지시문 받아서 수정된 서류 반환 → 바로 반영

### 상태 관리
- **회의록**: DB에 저장됨 → 수정 시 PATCH로 저장
- **나머지 7종**: 로컬 상태만 (페이지 벗어나면 사라짐). 필요하면 "이 버전 저장" 버튼 추가해 별도 테이블(`saved_documents`)로 선택적 저장

### UI
```
[서류 프리뷰]
...본문...
────────────────────
[✏️ 직접 편집] [🔄 재생성] [🖨 인쇄] [📋 복사]

┌ AI로 수정 ─────────────────────┐
│ 어떤 부분을 어떻게 바꿀까요?      │
│ [________________________]    │
│                    [적용]       │
└─────────────────────────────────┘
```

## AI 규칙 (기존 재사용)
- `NO_HALLUCINATION_RULES`: 입력 외 창작 금지
- `PERSONA_WRITING_RULES`: 50대 사회복지사 평어체
- `LOW_TEMP_GENERATION_CONFIG`: temperature 0.2
- JSON 응답 강제 (responseMimeType)

## 현재 UI 배치 (/documents)
```
┌ 서류 ──────────────────────────┐
│                                │
│  [대상자 서류]                  │
│   ▣ 욕구사정지  ▣ 급여제공계획서 │
│   ▣ 상태기록지  ▣ 급여제공기록지 │
│   ▣ 인수인계서                  │
│                                │
│  [요양사 서류]                  │
│   ▣ 상담일지 월간보고            │
│                                │
│  [매칭 서류]                    │
│   ▣ 서비스제공 계약서            │
│   ▣ 월간 업무보고서              │
│                                │
│  ───────────────               │
│  [회의록] + 새 회의록 버튼       │
│   · 2026-04-14 월례 직원회의     │
│   · 2026-04-10 사례회의 (김영희)  │
└────────────────────────────────┘
```

## 작업 순서
1. **계획 문서 확정** ← 지금
2. 백엔드: `/api/documents/generate` POST 구현
3. 프리뷰 페이지(`/documents/preview`) 구현 + print CSS
4. 입력 모달(PickerDialog) — 타입별 필요 입력 다름
5. `/documents` 카드 레이아웃 추가
6. 테스트: 8종 각각 실제 데이터로 돌려보고 출력 확인
7. 배포

## 검토 포인트 (네가 확인해줬으면 하는 것)
- [ ] 서류 8종 목록이 필요/충분한가 (빠진 서류, 지금 안 쓰는 서류?)
- [ ] 저장 안 하고 즉석 생성 방식 괜찮은가 (나중에 이력 필요하면 테이블 추가)
- [ ] 출력은 브라우저 인쇄만으로 충분한가 (PDF 다운로드 버튼 따로 필요?)
- [ ] 서류 UI 배치 (카테고리별 카드 그리드)
- [x] AI 생성 후 수정 → **직접 편집 + 채팅식 자연어 지시 둘 다 지원**
